import { AtlpFileDragDropDirective } from './atlp-file-drag-drop.directive';

describe('AtlpFileDragDropDirective', () => {
  it('should create an instance', () => {
    const directive = new AtlpFileDragDropDirective();
    expect(directive).toBeTruthy();
  });
});
